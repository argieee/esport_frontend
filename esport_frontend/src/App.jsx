import React, { useState } from 'react';import { Routes, Route, Navigate, useNavigate } from 'react-router-dom';import { ErrorBoundary } from './ErrorBoundary.jsx';import Home from './screens/landing/Home.jsx';import AboutUs from './screens/landing/AboutUs.jsx';import Projects from './screens/landing/Projects.jsx';import Services from './screens/landing/Services.jsx';import MainUIScreen from "./screens/main/MainUIScreen.jsx"; const App = () => {  const navigate = useNavigate();  const [isLoggedIn, setIsLoggedIn] = useState(() => {    return localStorage.getItem('isLoggedIn') === 'true';  });  const handleLoginSuccess = () => {    setIsLoggedIn(true);    localStorage.setItem('isLoggedIn', 'true');    navigate('/dashboard');   };  const handleLogout = () => {
    setIsLoggedIn(false);
    localStorage.removeItem('isLoggedIn');
    localStorage.removeItem('token');
    localStorage.removeItem('supabase.auth.token');
    navigate('/'); 
  };  return (    <Routes>      <Route path="/" element={<Home onLoginSuccess={handleLoginSuccess} />} />      <Route path="/about" element={<AboutUs onLoginSuccess={handleLoginSuccess} />} />      <Route path="/projects" element={<Projects onLoginSuccess={handleLoginSuccess} />} />      <Route path="/services" element={<Services onLoginSuccess={handleLoginSuccess} />} />      <Route        path="/dashboard"        element={          isLoggedIn ? (            <ErrorBoundary>              <MainUIScreen onLogout={handleLogout} />            </ErrorBoundary>          ) : (            <Navigate to="/" replace />          )        }      />      <Route path="*" element={<Navigate to="/" replace />} />    </Routes>  );};export default App;
